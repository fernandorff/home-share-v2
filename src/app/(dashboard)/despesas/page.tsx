import { SoonPage } from '../_components/SoonPage'

export default function DespesasPage() {
  return (
    <SoonPage
      title="Tela de despesas completa em breve."
      description="Por enquanto o painel principal já mostra todas as despesas do grupo com as visões Tabela e Por pessoa."
    />
  )
}
